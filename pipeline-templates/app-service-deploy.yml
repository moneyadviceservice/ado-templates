jobs:
  - job: Deploy
    displayName: 'Deploy to $(appServiceName)'
    pool:
      vmImage: $(vmImageName)
    steps:
      - task: DownloadPipelineArtifact@2
        displayName: 'Download Build Artifact'
        inputs:
          buildType: 'current'
          artifactName: 'drop'
          targetPath: '$(Pipeline.Workspace)/drop'

      - script: |
          AGENT_IP=$(curl -s https://api.ipify.org)

          echo "$AGENT_IP"

          TIMESTAMP=$(date +%H%M%S)
          PRIORITY=6$(date +%M%S)

          az network nsg rule create --resource-group maps-hub-prod-${{ parameters.env }} --nsg-name mhpd-app-nsg-${{ parameters.env }} --name allow-agent-ip-$TIMESTAMP --priority $PRIORITY --access Allow --direction Inbound --protocol "*" --source-address-prefixes $AGENT_IP --source-port-ranges "*" --destination-address-prefixes "*" --destination-port-ranges "*"
        displayName: 'Whitelist Agent IP'
          
      - task: AzureWebApp@1
        displayName: 'Azure App Service Deploy: $(appServiceName)'
        inputs:
          azureSubscription: '$(subscription)'
          appType: 'webAppLinux'
          appName: '$(appServiceName)'
          resourceGroupName: '$(resourceGroupName)'
          package: '$(Pipeline.Workspace)/drop/${{ parameters.packagePath }}.zip'
          runtimeStack: 'DOTNETCORE|8.0'

      - task: AzureWebApp@1
        inputs:
          azureSubscription: '$(subscription)'
          appType: webAppLinux #webApp for Windows
          appName: '$(appServiceName)'
          deployToSlotOrASE: true
          resourceGroupName: '$(resourceGroupName)'
          slotName: '$(slotName)'
          package: '$(Pipeline.Workspace)/drop/${{ parameters.packagePath }}.zip'

      - task: AzureAppServiceManage@0
        inputs:
          azureSubscription: '$(subscription)'
          appType: webAppLinux
          WebAppName: '$(appServiceName)'
          ResourceGroupName: '$(resourceGroupName)'
          SourceSlot: '$(slotName)'
          SwapWithProduction: true